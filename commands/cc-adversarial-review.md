---
description: "3-engine adversarial debate: Claude × Codex × Gemini battle. Usually auto-triggered via /cc-review. TRIGGER: 'adversarial review', 'debate review', '对抗审查'."
---

Run 3-engine adversarial debate: `cc-flow adversarial-review`

Normally you don't need this directly — `/cc-review` auto-escalates to debate mode when 3 engines are available.

Options: `--engines claude,gemini` `--range HEAD~5` `--path scripts/` `--dry-run`
