---
description: "Dependency graph — blocking chains, parallel phases, critical path, what to work on next. TRIGGER: 'dependencies', 'what blocks', 'critical path', '依赖关系'"
---

Activate the cc-deps skill.

Analyze epic/task dependencies, compute parallel execution phases, identify the critical path, and recommend which tasks to start next.
