---
description: >
  Pythonic idioms, PEP 8, type hints, and best practices for robust Python applications.
  TRIGGER: 'python patterns', 'PEP 8', 'type hints', 'idiomatic python', 'pythonic'.
  NOT FOR: async patterns, testing, deployment.
---

Activate the cc-python-patterns skill.

## Covers

- Type hints (Python 3.9+ built-in types, Protocol, Union)
- Error handling (specific exceptions, chaining, custom hierarchy)
- Context managers (resources, custom with contextlib)
- Data classes (validation in __post_init__)
- Concurrency (threading, multiprocessing, thread safety)
- Anti-patterns to avoid (mutable defaults, bare except, string concat in loops)
- Project layout and tooling (black, isort, ruff, mypy, pytest, bandit)
