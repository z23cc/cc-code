---
description: "3-engine collaborative plan: Claude designs → Codex critiques → Gemini synthesizes. TRIGGER: 'multi plan', '3 engine plan', '多引擎规划'."
---

Run 3-engine collaborative planning: `cc-flow multi-plan "your goal"`

Or use `cc-flow autopilot "goal"` for the full guided pipeline (plan + execute + checkpoint + review).

Options: `--timeout 300` `--dry-run`
